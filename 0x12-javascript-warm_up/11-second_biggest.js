#!/usr/bin/node
let sort;
const args = process.argv.slice(2);
if (args.length === 0 || args.length === 1) {
  console.log(0);
} else {
  for (let i = 0; i < args.lenght; i++) {
    if (args[i]
  }

  args.reverse();
  console.log(args[1]);
}
